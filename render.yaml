services:
  - type: web
    name: signex-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn ntro_sigint.server.app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0